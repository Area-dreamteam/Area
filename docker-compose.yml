services:

  server:
    env_file: "./server/.env"
    ports:
      - "8080:8080"
    build:
      context: "./server"
    volumes:
      - "./server/app:/server/app"
      - "sqlite-data:/server/app"
    restart: always
    networks:
      - area_net

  # client_web:
  #   container_name: client_web
  #   env_file: "./web/.env"
  #   ports:
  #     - "8081:8081"
  #   volumes:
  #     - "shared-volume:/shared"
  #   restart: always
  #   build:
  #     context: "./web"
  #   networks:
  #     - area_net
  #   depends_on:
  #     - client_mobile
  #     - server

  # client_mobile:
  #   container_name: client_mobile
  #   env_file: "./mobile/.env"
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - "shared-volume:/shared"
  #   restart: always
  #   build:
  #     context: "./mobile"
  #   networks:
  #     - area_net
  #   depends_on:
  #     - server

volumes:
  sqlite-data:
  shared-volume:

networks:
  area_net:
