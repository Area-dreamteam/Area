import { IonPage, IonContent, IonText } from '@ionic/react';

export default function IsRegisted() {
  return (
    <IonPage>
      <IonContent className="ion-text-center ion-padding">
        <IonText color="primary">
          <h1>Registed</h1>
        </IonText>
      </IonContent>
    </IonPage>
  );
}
